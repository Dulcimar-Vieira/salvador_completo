name: Processar Feed XML

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  process-feed:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Configurar cache do pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Instalar Dependências
        run: pip install -r requirements.txt

      - name: Garantir permissão dos scripts
        run: chmod +x script.py merge_json.py

      - name: Garantir pasta json_parts
        run: |
          mkdir -p json_parts
          touch json_parts/.gitkeep

      - name: Limpar arquivos antigos
        run: |
          rm -f json_parts/*.json || true
          rm -f merged.json || true

      - name: Baixar e Processar Feed XML
        run: |
          echo "📥 Iniciando script.py..."
          python script.py || { echo "❌ Erro no script.py"; exit 1; }

      - name: Mesclar arquivos JSON
        run: |
          echo "🧩 Iniciando merge_json.py..."
          python merge_json.py || { echo "❌ Erro no merge_json.py"; exit 1; }

      - name: Verificar arquivos antes do commit
        run: |
          echo "📁 Conteúdo da pasta json_parts:"
          ls -lh json_parts/ || echo "⚠️ Pasta vazia!"
          echo "📄 Verificando merged.json:"
          ls -lh merged.json || echo "⚠️ Arquivo não gerado!"
          echo "🔍 Linhas em merged.json:"
          wc -l merged.json || echo "⚠️ Não foi possível contar linhas."

      - name: Configurar Git
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

      - name: Atualizar repositório antes do push
        run: git pull --rebase || echo "⚠️ Nada para atualizar"

      - name: Commit e Push dos Arquivos JSON
        run: |
          git add -f json_parts/
          git add -f merged.json
          git commit -m "🔄 Atualização automática dos arquivos JSON" || echo "⚠️ Nada para commitar"
          git push || echo "⚠️ Nenhuma alteração para enviar"

      - name: Verificar arquivos após commit
        run: |
          echo "✅ Pós-push: arquivos no repositório"
          ls -lh json_parts/
          ls -lh merged.json
